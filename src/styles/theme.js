const themeDark = {
  theme: 'dark',
}

const themeLight = {
  theme: 'light',
  primary: '#1fb6ff',
}

const customColors = {
  white: '#FFFFFF',
  black: '#000000'
}

export { themeDark, themeLight, customColors }
