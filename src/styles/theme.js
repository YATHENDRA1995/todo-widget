const themeDark = {
  theme: 'dark',
  primary: '#1fb6ff',
  bodyBackground: '#0c1014',
  textColor: '#000000'
}

const themeLight = {
  theme: 'light',
}

const customColors = {
  white: '#FFFFFF',
  black: '#000000'
}

export { themeDark, themeLight, customColors }
